spring:
  profiles:
    active: dev
  application:
    name: hello-sample

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,env
  endpoint:
    health:
      show-details: always